using this rust lib
#fetch https://crates.io/crates/ollama-rs#generate-embeddings

add in ollama_rs:

* Completion Generation (Streaming)
* Generate Embeddings, accept an array of strings and generate embeddings
* make it function tauri commands.

search in context 7 for more specific implemetation, package name 'ollama_rs'

keep it simple