/**
 * Route parameters interface for type-safe access to route params across the application
 */
export interface ChatRouteParams {
	id?: string;
	category?: string;
	chatId?: string;
	url?: string;
}
